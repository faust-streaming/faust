# Native OpenTelemetry tracing (see docs/adr/0001-native-opentelemetry-tracing.md).
# Faust's tracing core depends only on the API; the SDK is included so apps can
# configure an exporter out of the box.
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
